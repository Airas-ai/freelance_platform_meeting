[pytest]
pythonpath = .
testpaths = tests
markers =
    mysql: integration test requiring MYSQL_* credentials and a running MySQL server
    integration: needs DEEPSEEK_API_KEY and/or external services
